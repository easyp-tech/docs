---
title: "Справочник protobuf.mod"
description: "Спецификация файла protobuf.mod, независимого от инструментов DSL для объявления идентичности модулей Protocol Buffer, корневых каталогов, зависимостей и локальных замен."
---

Файл `protobuf.mod` располагается в корне proto-модуля. Задуманный как **tool-neutral стандарт**, он не содержит настроек конкретных инструментов (ни флагов линтеров, ни плагинов генерации). Файл сфокусирован исключительно на идентификации модуля, внутренних директориях, объявлениях зависимостей и локальных переопределениях источников.

---

## Синтаксис DSL и директивы

<TypeTable
  type={{
    module: {
      description: "Канонический URL или путь модуля (например, github.com/acme/user). Зарезервирован для vanity-путей.",
      type: "директива"
    },
    roots: {
      description: "Список внутренних поддиректорий репозитория с файлами .proto. По умолчанию '.'.",
      type: "директива"
    },
    require: {
      description: "Блок объявления прямых и транзитивных зависимостей с явными SemVer-версиями.",
      type: "блок директив"
    },
    "require // indirect": {
      description: "Метка транзитивной зависимости, управляемая автоматически командой 'easyp mod tidy'.",
      type: "аннотация"
    },
    replace: {
      description: "Директива локального переопределения, связывающая URL модуля с путем на диске или альтернативным репозиторием.",
      type: "директива"
    }
  }}
/>

---

## Пример спецификации

```protobuf
// proto/user/protobuf.mod

module github.com/acme/user

roots (
    proto
)

require (
    github.com/googleapis/googleapis v1.5.0
    github.com/acme/weather v1.2.3
    github.com/grpc-ecosystem/grpc-gateway v2.19.0 // indirect
)

// Локальное переопределения для отладки
replace github.com/acme/weather => ../weather-fork
```

---

## Правила поведения

1. **Git-Native идентичность**: Каноническим идентификатором модуля является его Git URL (`github.com/acme/weather`).
2. **Помодульный скоуп**: Каждый модуль имеет собственный `protobuf.mod`. MVS работает помодульно (per-module), не навязывая глобальных ограничений версий в монорепозитории.
3. **Защита в CI**: Активные директивы `replace` разрешены при локальной разработке, но вызывают ошибку при сборке в CI.
